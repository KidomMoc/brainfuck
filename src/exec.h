#ifndef EXE_H
#define EXE_H

#include <stdlib.h>
#include <stdio.h>
#include "memory.h"

/* Function: execute
 * code: the code to execute
 * return: status code (0 -> succeed, 1->failed)
 */

int execute (char *code);

#endif /* EXE_H */
